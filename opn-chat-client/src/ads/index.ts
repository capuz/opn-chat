export { adManager }        from './AdManager';
export type { AdsProvider } from './AdsProvider';
export { MODAL_TO_REWARD }  from './types';
export type {
  RewardType,
  RewardPayload,
  RewardContext,
  RewardResult,
  AdEvent,
  AdEventName,
  AdsProviderName,
} from './types';
